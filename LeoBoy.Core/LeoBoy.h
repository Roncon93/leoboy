#pragma once  // Ensures the file is included only once during compilation

// Declare a function that adds two integers and returns the result
extern "C" __declspec(dllexport) int Add(int a, int b);
